import { ComponentFixture, TestBed } from '@angular/core/testing';

import { MarkAssignment } from './mark';

describe('MarkAssignmentComponent', () => {
  let component: MarkAssignment;
  let fixture: ComponentFixture<MarkAssignment>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ MarkAssignment ]
    })
    .compileComponents();
  });

});
